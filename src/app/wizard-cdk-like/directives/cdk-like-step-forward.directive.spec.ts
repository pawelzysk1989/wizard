import { CdkLikeStepForwardDirective } from './cdk-like-step-forward.directive';

describe('CdkLikeStepForwardDirective', () => {
  it('should create an instance', () => {
    const directive = new CdkLikeStepForwardDirective();
    expect(directive).toBeTruthy();
  });
});
