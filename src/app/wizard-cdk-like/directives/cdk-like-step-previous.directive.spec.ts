import { CdkLikeStepPreviousDirective } from './cdk-like-step-previous.directive';

describe('CdkLikeStepPreviousDirective', () => {
  it('should create an instance', () => {
    const directive = new CdkLikeStepPreviousDirective();
    expect(directive).toBeTruthy();
  });
});
