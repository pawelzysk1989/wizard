import { WizardPreviousStepDirective } from './wizard-previous-step.directive';

describe('WizardPreviousStepDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardPreviousStepDirective();
    expect(directive).toBeTruthy();
  });
});
