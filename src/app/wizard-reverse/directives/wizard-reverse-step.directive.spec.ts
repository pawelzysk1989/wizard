import { WizardReverseStepDirective } from './wizard-reverse-step.directive';

describe('WizardReverseStepDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardReverseStepDirective();
    expect(directive).toBeTruthy();
  });
});
