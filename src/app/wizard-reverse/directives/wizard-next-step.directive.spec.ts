import { WizardNextStepDirective } from './wizard-next-step.directive';

describe('WizardNextStepDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardNextStepDirective();
    expect(directive).toBeTruthy();
  });
});
